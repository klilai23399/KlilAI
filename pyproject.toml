[project]
name = "klilai"
version = "0.1.0"
description = "KlilAI workspace using notebooklm-py."
requires-python = ">=3.10"
dependencies = [
    "notebooklm-py==0.4.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
